#include <stdio.h>
#include <stdlib.h>

/* Fonction située à l'adresse 0x08048444 */
void run() {
    fwrite("Good... Wait what?\n", 1, 19, stdout);
    system("/bin/sh");
}

int main() {
    char buffer[64]; // Espace alloué sur la pile
    gets(buffer);    // Vulnérabilité : pas de vérification de taille
    return 0;
}